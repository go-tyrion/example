; 服务名称，可用于 consul 注册
service_name = user

; 服务地址
addr = :8080

; access log
; on, off
access_log = on

access_log_rotate = D

; read timeout
read_timeout_ms = 30000

; write timeout
write_timeout_ms = 30000

;
max_post_memory = 32M

https_cert_file =

https_key_file =

[prod]
addr = :3210
